{
  "name": "@craftgen/database",
  "version": "0.0.1",
  "description": "",
  "main": "mod.ts",
  "scripts": {
    "drizzle:generate": "drizzle-kit generate --out ./primary/migrations --breakpoints --schema=./primary/schema/index.ts --dialect=sqlite",
    "drizzle:generate:orgs": "drizzle-kit generate --out ./tenant/migrations-orgs --breakpoints --schema=./tenant/schema/index.ts --dialect=sqlite",
    "drizzle:migrate": "pnpm with-env tsx drizzle/migrate.ts",
    "drizzle:push": "pnpm with-env drizzle-kit push",
    "with-env": "dotenv -e ../.env --",
    "with-env:prod": "dotenv -e ../.env.prod --",
    "studio": "drizzle-kit studio --config ./tenant/drizzle.config.ts",
    "push_demo": "APP_NAME=necmttn deno run -A ./lib/create-org-db.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@effect/schema": "^0.71.0",
    "@libsql/client": "^0.7.0",
    "@paralleldrive/cuid2": "^2.2.2",
    "@tursodatabase/api": "^1.8.1",
    "drizzle-orm": "^0.32.0",
    "effect": "^3.6.1",
    "ky": "^1.1.3"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "drizzle-kit": "^0.23.0",
    "tsx": "^3.12.7"
  }
}
